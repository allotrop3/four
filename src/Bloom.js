'use strict';

import Structure from './Structure';

class Bloom extends Structure
{
    constructor(
    {
        name = 'bloom'
    } = {})
    {
        super({ name });
    }
}

export default Bloom;
