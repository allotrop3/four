'use strict';

let dependencies = [];

let VERTEX_SHADER = [
].join('\n');

let FRAGMENT_SHADER = [
].join('\n');

module.exports = {
   dependencies: dependencies,
   VERTEX_SHADER: VERTEX_SHADER,
   FRAGMENT_SHADER: FRAGMENT_SHADER
};